{
  "name": "noble-essence",
  "version": "1.0.0",
  "description": "Premium e-commerce platform for luxury pillow cases",
  "private": true,
  "workspaces": [
    "storefront",
    "admin",
    "functions"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
